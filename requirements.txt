# Desktop app + engine (LM Studio, lights, Gmail)
requests>=2.28.0
nanoleafapi>=2.1.0

# API server (optional: only needed for remote Android / web client)
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# D&D improv: read .odt campaign notes
odfpy>=1.0.0

# D&D RAG: local embeddings for retrieval
sentence-transformers>=2.2.0
numpy>=1.24.0
